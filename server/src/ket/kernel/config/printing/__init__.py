"""Mẫu in chứng từ (FR-RPT-008) — phần METADATA, ở kernel cùng lý do
`config/reports`: provision phải gieo mẫu builtin (C1 cấm kernel→reporting);
phần THI HÀNH (render + print_log + kiểm soát in FR-RPT-011) ở
`reporting/printing`."""
