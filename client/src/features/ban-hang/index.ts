/**
 * Nhóm màn hình: Bán hàng & công nợ phải thu
 *
 * SRS: 06 · Module backend: sales + receivables + pricing · Phase: 7 (lát 7H-2a)
 *
 * Thư mục theo NHÓM MÀN HÌNH của design, không theo ranh giới module backend
 * (xem bảng "IA màn hình ≠ ranh giới module" trong docs/system-architecture.md).
 * Dùng lại bộ xương nhóm 01 (`mua-hang`) — design không vẽ màn bán riêng.
 */

export { SalesInvoiceForm } from './sales-invoice-form'
export { SalesListPage } from './sales-list-page'
