/**
 * Type sinh từ OpenAPI của app server — KHÔNG viết tay vào thư mục này.
 *
 * Hợp đồng client↔server là REST + OpenAPI (LD-03), nên type ở đây phải được
 * sinh lại mỗi khi server đổi contract. Bước sinh (openapi-typescript) gắn vào
 * CI ở phase 2, khi server đã có endpoint nghiệp vụ đầu tiên.
 *
 * Phase 1 cố ý rỗng.
 */

export {}
